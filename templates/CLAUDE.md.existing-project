# [Project Name]

[2-3 sentences: what it does, who uses it, what problem it solves. Be specific.]

## Quick context

- **Type**: [web app / API service / CLI / library / monorepo]
- **Stack**: [language + version, framework + version, key libraries]
- **Database**: [e.g., PostgreSQL 15 + Redis for cache]
- **Deployment**: [e.g., Kubernetes on GKE / AWS ECS / Heroku]
- **Repo structure**: [single service / monorepo with packages/, services/]
- **Phase**: [Active development / Maintenance / Migration]

## How to work in this codebase

- Read `docs/patterns/coding-patterns.md` BEFORE implementing — patterns are established
- Read `docs/architecture/adr/README.md` before making architectural changes
- Run `/architect` for decisions with multi-week implications
- Run `/implement` for pattern-aware implementation help

## Key facts

[5-10 non-obvious, critical facts. Things that would bite a new contributor who didn't know them.]

1. [e.g., Auth tokens are validated in middleware/auth.go — services downstream receive a pre-validated user struct]
2. [e.g., All database writes go through the Repository layer — never write SQL outside internal/repository/]
3. [e.g., Background jobs use Bull queue — never do async work inline in HTTP handlers]
4. [e.g., The shared/ package is imported by all services — changes have wide blast radius]
5. [e.g., Feature flags are in config/features.go — check before adding env-based conditionals]

## Documentation map

| Want to understand... | Read... |
|---|---|
| System topology and data flows | `docs/architecture/overview.md` |
| All architectural decisions | `docs/architecture/adr/README.md` |
| Specific component | `docs/components/[name].md` |
| All components at a glance | `docs/components/README.md` |
| How to write a new [route/model/job] | `docs/patterns/coding-patterns.md` |

## Commands

```bash
# Replace with actual commands:
make dev            # Start development server
make test           # Run test suite
make lint           # Lint and type-check
make build          # Production build
make migrate        # Run database migrations
```

## Directory map

```
[project-name]/
├── [dir 1]     # [what lives here]
├── [dir 2]     # [what lives here]
├── [dir 3]     # [what lives here]
└── docs/       # Architecture (ADRs + overview), component docs, patterns
```

---

_Generated by ai-sdlc /onboard. See [VERIFY] markers in docs/ for items requiring human confirmation._
