# Google Cloud Run Environment
ENVIRONMENT=cloud

# Google Cloud Project ID
GOOGLE_CLOUD_PROJECT=apa-bd

# Google Cloud Storage Bucket
# Format: {PROJECT_ID}-tv-programa
GCS_BUCKET_NAME=tv-programa

# Cloud Run Port (Cloud Run sets PORT env var, but just to be explicit)
PORT=8080
