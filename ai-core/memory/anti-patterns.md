# Anti-Patterns

Approaches that have been tried and caused problems. Load this before implementing to avoid known failure modes.

Entries here can trigger an ADR (if they reveal a missing architectural constraint) or enrich `ai-core/knowledge/patterns/` with a "what not to do" example.

**Format:**
```
[YYYY-MM-DD] FAILED: <what was tried> — <why it failed> [Session: <skill> / <task>]
[See ADR-XXXX] (if promoted to a formal decision)
```

---

<!-- Entries will be appended here by summarize-failure.sh and reviewer skill -->
